# EventLoop
JS is synchronous and single threaded bnudefault 
 ## There can be async behaviour 
 - with browerAPI -setTime, setInterval,
 setImmediate, nextTick
 -with promises 
 - with event handlers
 promise- a fxn not executed imediately but it must be executed after a while .it has some status during the execution . at final it may  resolve 
 Morden js divided into 2 category 
 1. Common js (cjs)-> supports OOps -> required 
 2.Module js (mjs)-> follow modular approach -> import